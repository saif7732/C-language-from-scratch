#include <stdio.h>

int main(){

    // 3. A do while loop is executed:

    int i=0;
    do{
        printf("hello world");
        i++;
    }
    while(0);  //condition is false, hence the while loop will run once

    return 0;
}